#######################
Version Notes for 3.4.4
#######################

We fixed a bug where invalid ``category/category-names`` in the URL did not throw ``{if no_results}``. These requests will now 404.
